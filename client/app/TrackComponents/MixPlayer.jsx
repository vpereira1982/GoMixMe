import React from 'React';
import ReactDOM from 'React-DOM';

const MixPlayer = (props) => (
  <div className="mix_player">
    <div className="mix_info">
      what the fuck?
    </div>
  </div>

)

export default MixPlayer;